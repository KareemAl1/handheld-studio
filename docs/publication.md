# GitHub and Vercel preparation

Prepared locally on **2026-09-14**. This report is a reviewable publication proposal, not a deployment record. Repository creation, pushing and deployment are still pending final user approval.

## Proposed destinations

| Setting | Proposed / verified value |
| --- | --- |
| Repository name | `handheld-studio` |
| GitHub owner | **KareemAl1** — active CLI account and authenticated API identity verified |
| Repository visibility | **Public** |
| GitHub target | `KareemAl1/handheld-studio`; authenticated lookup found no existing repository |
| Vercel account | **kareemal1** — verified by CLI |
| Vercel workspace | **kareems-projects-bc520863** (`kareem's projects`) — selected by the owner and access verified |
| Vercel project name | `handheld-studio`; scoped lookup found no existing project |
| Production branch | `main` |
| Environment variables | None required |

No Git remote or Vercel project link has been created. Before any authenticated GitHub action, verify that the active account is still KareemAl1. The repository's [AGENTS.md](../AGENTS.md) records this account boundary and the publication approval requirement.

Existing commit metadata uses the approved author name and the configured personal email. That email will be visible if the history is published. GitHub's public profile returned no email, so association with the account could not be verified from the available profile data. No commit authors, timestamps or history were rewritten.

## File and history review

A read-only review covered all 167 files tracked before preparation and 202 unique reachable Git blobs. The starting checkout held 38.03 MB, including 34.84 MB of verification artifacts; unique historical blob contents totaled 26.21 MB before this preparation commit.

- Token/private-key/credential-URL pattern scans found **no likely secrets** in current or reachable historical text. This is a targeted review, not a formal secret-scanning certification.
- No dependency directories, build output, caches, environment files or credential stores are tracked. `.npmrc` contains only project-local cache and npm preferences. `.vercel/` is now explicitly ignored.
- The largest tracked asset is `public/models/hs-01.glb`, **1,801,256 bytes**. The editable `assets/source/hs-01.blend`, **817,197 bytes**, remains tracked with the authoring/export scripts. No oversized blob or need for a source-asset conversion was identified.
- The GLB has no external URIs or absolute paths. Reviewed PNGs have no text/EXIF metadata. Read-only Blender inspection found no external assets, linked libraries or embedded scripts.
- **17 redundant generated PNGs, 14,240,832 bytes**, were removed from tracking and ignored. They remain available locally and are regenerated by export tests; none are baseline fixtures or Markdown link targets. Curated screenshots, product images and JSON measurement records stay in Git.
- Historical blobs remain reachable. Removing duplicate exports from the current tree does not rewrite or erase earlier commits; Git already deduplicates identical blob contents.

The README now includes actual screenshots, features, setup, controls, engineering tradeoffs, the editable asset workflow, credits and explicit testing limits. Operational authentication history was removed from the public-facing README. A small hosted-site copy adjustment replaces the localhost warning with the correct current-site explanation when the app runs on a public hostname.

## Dependency notices and licensing

The minified runtime bundles did not retain dependency copyright/license text. [third-party-notices.txt](../public/third-party-notices.txt) now ships with the static website. Its offline [generator](../scripts/third-party-notices.mjs) checks the installed dependency versions against the lockfile, retains licenses/copyright/NOTICE content and includes documented, version-pinned upstream supplements. Build fails if notices drift.

The final notice covers **67 non-dev package entries, 59 distinct texts, 128,485 bytes**. It conservatively includes some helpers removed by tree shaking. The generator records upstream packaging gaps: maath and stats-gl declare MIT but omit license/copyright files; no copyright lines were invented. MediaPipe's canonical Apache terms are accompanied by installed source headers, with the unavailable matching source tag documented. Reference portfolio provenance remains in [references.md](references.md).

No project-wide license has been selected for the original code or assets. Dependency notices do not select one on the owner's behalf.

## Vercel build settings

[vercel.json](../vercel.json) records Vite, install `npm ci`, build `npm run build`, and output `dist`. `package.json` and `.nvmrc` select Node 22.x; use at least 22.12 locally for the installed Vite release. This is a static site with no backend or required secrets.

Shared configurations live in the query string on `/`. There are no client-side pathname routes, so no blanket SPA rewrite was added. The static model, poster and notices retain their real file paths. Only `dist/` is served; it contains no Blender source, tests, documentation, local cache or development diagnostics.

[.vercelignore](../.vercelignore) also excludes unnecessary authoring/test/cache files from CLI uploads while retaining the notices generator needed by the build. Git still retains the editable source. These choices follow the official [Vite integration](https://vercel.com/docs/frameworks/frontend/vite), [build/output configuration](https://vercel.com/docs/builds/configure-a-build), [Node version settings](https://vercel.com/docs/functions/runtimes/node-js/node-js-versions) and [deployment exclusions](https://vercel.com/docs/deployments/vercel-ignore).

## Fresh verification

| Check | Actual result |
| --- | --- |
| Dependency notices | Offline generation and `--check` pass; final build used the complete notice file |
| TypeScript + Vite production build | Passed; 613 modules, static output generated |
| ESLint | Passed |
| Unit tests | **162 passed** across 7 files |
| README/report links and deployment exclusions | Passed; required model, app, lockfile and notices generator remain in the upload inputs |
| Production dependency audit | **0 reported vulnerabilities** from `npm audit --omit=dev --json` |
| Production Chrome smoke test | **Passed**, Chrome 152.0.7977.83, 11.94 seconds |
| Console and network | No page errors, console errors, failed requests or HTTP errors in that production run |
| Production PNG | Actual download decoded to opaque **1600 × 1200**, 997,385 bytes |

The production flow verified the actual game screen, collection, game over, restart, Exit, configuration save/reload, shared URL precedence over a different saved build and image export. Saved and shared rendered configurations matched their reference under the decoded-pixel comparison. New evidence is stored separately: [production results](screenshots/publication/production-smoke.json), [desktop](screenshots/publication/production-desktop.png), [game](screenshots/publication/production-game.png), [export](screenshots/publication/production-export.png). Earlier milestone evidence was preserved.

The Three chunk remains **709.79 kB minified / 183.03 kB gzip**, and Vite's generic chunk-size warning remains visible. This preparation did not change geometry or claim a new frame rate. Detailed desktop/mobile/interaction results remain in [milestone-3.md](milestone-3.md); that full suite was not repeated for this documentation/deployment preparation.

Vercel-hosted routing, HTTPS clipboard/download permissions and real-device behavior cannot be reported as verified before deployment. A local production build is not a Vercel deployment. Physical-phone/Safari tests and native hidden-tab checks remain the documented limitations from milestone 3.

## Approval boundary

The proposed next action, only after explicit final approval, is to create public `KareemAl1/handheld-studio`, push the reviewed local `main` history, and create/deploy Vercel project `handheld-studio` under `kareems-projects-bc520863`. Verify both identities again before those actions. Do not operate on any sibling project or use a different GitHub account.

After an approved deployment, inspect the HTTPS site and test the real share link, storage, game and PNG download there before calling publication complete.
